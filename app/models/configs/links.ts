const links = () => {

    const linksData = [
        {
            name: 'Portfolio',
            url: '/portfolio',
            type: 'internal'
        }
    ]

    return linksData ?? null
}

export default links